# Template

Example configuration for IDA Patcher.

## Usage

1. Copy `ida-patcher.json` to your IDA Pro `plugins` directory
2. Modify the patterns to match your use case
3. Set `enabled` to `true` when ready to apply patches
